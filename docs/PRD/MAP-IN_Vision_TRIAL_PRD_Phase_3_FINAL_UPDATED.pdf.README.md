# MAP-IN Vision (TRIAL) — PRD Phase 3

The approved engineering baseline is the PDF supplied with this project/conversation:
`MAP-IN_Vision_TRIAL_PRD_Phase_3_FINAL_UPDATED.pdf`

Key locked workflow:

`OPEN CAMERA → LIVE → CAPTURE → MEMORY IMAGE → MASTERING → ADD TOOLS → TOOL SETTING → LEVEL / THRESHOLD ADJUSTMENT → I/O SETTING → TEST ↔ ADJUST → SAVE / LOCK RECIPE → RUN`

Runtime principles:
- Offline-first; no Internet required for inspection runtime.
- Local storage for images, recipes, models, results, logs, and audit records.
- Industrial camera + USB UVC webcam support.
- PLC + Arduino Uno via USB through abstraction layers.
- Laptop/PC is the main development station; Jetson Xavier NX 8 GB is the edge deployment/validation target.
- User-facing branding: `MAP-IN Vision (TRIAL)`.

Do not infer missing requirements. If the PDF is not present in the repository, stop and ask for it rather than inventing details.
